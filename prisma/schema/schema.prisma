generator client {
  provider     = "prisma-client"
  output       = "../../src/lib/generated/prisma"
  moduleFormat = "esm"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
